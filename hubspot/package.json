{
  "name": "spense-hubspot",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "test": "vitest run",
    "test:watch": "vitest",
    "sample": "tsx examples/render-sample.ts"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20240924.0",
    "tsx": "^4.19.0",
    "typescript": "^5.5.4",
    "vitest": "^2.1.1",
    "wrangler": "^4.0.0"
  }
}
